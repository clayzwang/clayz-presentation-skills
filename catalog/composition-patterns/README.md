# Composition Patterns

This directory contains original, brand-neutral semantic-to-spatial methods.
The JSON records contain no coordinates, themes, visual variants, templates,
assets, copy, fonts, or model features. A pattern may be selected only through
the public index and its linked Failure Patterns require a second receipt.
